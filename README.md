# OnlineTest - Платформа онлайн обучения

Веб-приложение для создания и прохождения онлайн-курсов с системой тестирования и отслеживания прогресса студентов.

## Возможности

### Для студентов:
- 📚 Прохождение курсов с модульной структурой
- ✅ Интерактивные тесты (обычные и с проверкой кода)
- 📊 Отслеживание прогресса обучения
- 🎓 Система уровней и опыта (XP)
- 👤 Личный профиль с аватаром
- 🔐 Регистрация через email или Google OAuth

### Для преподавателей:
- ➕ Создание и редактирование курсов, модулей и уроков
- 📝 Управление тестами с вопросами разных типов
- 💻 Создание code-тестов с автоматической проверкой
- 📈 Просмотр статистики студентов
- ✏️ Редактирование контента прямо со страницы

## Технологический стек

- **Backend:** Django 6.0, Python 3.14
- **Frontend:** HTML, CSS, JavaScript (Vanilla)
- **Database:** SQLite (dev) / PostgreSQL (production ready)
- **Task Queue:** Celery + RabbitMQ (для проверки кода)
- **Authentication:** Django Allauth (Email + Google OAuth)
- **Deployment:** Ready for production with WSGI/ASGI

## Установка и запуск

### 1. Клонирование репозитория
```bash
git clone https://github.com/LarEarl/OnlineTest.git
cd OnlineTest
```

### 2. Создание виртуального окружения
```bash
python -m venv .venv

# Windows
.venv\Scripts\activate

# Linux/Mac
source .venv/bin/activate
```

### 3. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 4. Настройка базы данных
```bash
python manage.py migrate
```

### 5. Создание суперпользователя
```bash
python manage.py createsuperuser
```

### 6. (Опционально) Создание демо-курса
```bash
python create_demo_course.py
```

### 7. Запуск сервера разработки
```bash
python manage.py runserver
```

Приложение будет доступно по адресу: `http://127.0.0.1:8000/`

## Настройка Google OAuth (опционально)

1. Создайте проект в [Google Cloud Console](https://console.cloud.google.com/)
2. Настройте OAuth 2.0 credentials
3. Добавьте Authorized redirect URI: `http://127.0.0.1:8000/accounts/google/login/callback/`
4. Запустите команду настройки:
```bash
python manage.py setup_google_oauth
```

## Настройка Celery для проверки кода (опционально)

### 1. Установка RabbitMQ
```bash
# Windows (с помощью Chocolatey)
choco install rabbitmq

# Linux (Ubuntu/Debian)
sudo apt-get install rabbitmq-server

# Mac
brew install rabbitmq
```

### 2. Запуск Celery worker
```bash
celery -A OnlineTest worker -l info
```

## Структура проекта

```
OnlineTest/
├── courses/          # Управление курсами, модулями и уроками
├── tests_app/        # Система тестирования
├── users/            # Аутентификация и профили
├── progress/         # Отслеживание прогресса
├── teacher_app/      # Панель управления для преподавателей
├── templates/        # HTML шаблоны
├── static/           # CSS, JS, изображения
├── media/            # Загружаемые файлы пользователей
└── OnlineTest/       # Настройки проекта
```

## Основные URL-маршруты

- `/` - Главная страница
- `/users/login/` - Вход
- `/users/register/` - Регистрация
- `/courses/` - Список курсов
- `/tests_app/` - Доступные тесты
- `/progress/` - Мой прогресс
- `/teacher/` - Панель преподавателя (требует прав администратора)
- `/admin/` - Админ-панель Django

## Развертывание в production

### Необходимые шаги:

1. **Измените SECRET_KEY** в `settings.py`
2. **Установите DEBUG = False**
3. **Настройте ALLOWED_HOSTS**
4. **Настройте PostgreSQL:**
   ```python
   DATABASES = {
       'default': {
           'ENGINE': 'django.db.backends.postgresql',
           'NAME': 'onlinetest_db',
           'USER': 'your_user',
           'PASSWORD': 'your_password',
           'HOST': 'localhost',
           'PORT': '5432',
       }
   }
   ```
5. **Соберите статические файлы:**
   ```bash
   python manage.py collectstatic
   ```
6. **Настройте WSGI/ASGI сервер** (Gunicorn, uWSGI)
7. **Настройте веб-сервер** (Nginx, Apache)

## Создание администратора курса

```bash
python manage.py shell
```
```python
from users.models import User
user = User.objects.get(username='имя_пользователя')
user.is_course_admin = True
user.save()
```

## Лицензия

MIT License

## Автор

LarEarl - [GitHub](https://github.com/LarEarl)

## Поддержка

Если у вас возникли вопросы или проблемы, создайте [issue](https://github.com/LarEarl/OnlineTest/issues) в репозитории.
